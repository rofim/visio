const ErrorCode = {
  DeviceAccess: 'device.access.error',
  DevicesTrackUnavailable: 'devices.track.unavailable',
  FacingModeNotSupported: 'facingMode.not.supported',
} as const;

export default ErrorCode;
